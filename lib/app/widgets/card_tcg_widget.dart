import 'package:flutter/material.dart';

class CardTcgWidget extends StatelessWidget {
  const CardTcgWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
